# Container build instructions for the Python AI service will go here.
# Python AI 服务的容器构建说明后续写在这里。
# Keep the image focused on the API runtime and runtime dependencies only.
# 镜像应只保留 API 运行环境和必要依赖，避免混入无关内容。
