# Python dependencies for the AI service will be added here later.
# Python AI 服务的依赖将后续添加到这里。
# Keep this file as the single source of truth for runtime packages.
# 将此文件作为运行时依赖的唯一维护来源。
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-dotenv>=1.0,<2
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
langchain-core>=0.3,<0.4
langchain-openai>=0.3,<0.4
langsmith>=0.4,<1
SQLAlchemy>=2.0,<3
PyMySQL>=1.1,<2
redis>=5.0,<6
pymilvus>=2.6,<3
httpx>=0.28,<1
